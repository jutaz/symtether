# Markdown is hand-authored and has never been prettier-formatted; pointing
# prettier at it would reflow README/SPEC/docs. The AGENTS.md managed block is
# a prettier fixed point (see the regression test in test/init.test.ts), so
# this ignore no longer hides an init-vs-prettier mismatch.
*.md
.scratch/
dist/
coverage/
grammars/
node_modules/
test/fixtures/
package-lock.json

# Vendored verbatim from the symtether design system source of truth.
# Do not reformat — it would drift from upstream.
docs/.vitepress/theme/tokens/
