# Never run install scripts: the tree-sitter grammar packages are
# devDependencies whose postinstall would node-gyp-compile native bindings
# we never use (we only extract their prebuilt WASM). This also covers
# environments that run plain `npm ci` without flags (Cloudflare Workers
# Builds), and kills install-time supply-chain scripts as a side effect.
ignore-scripts=true
